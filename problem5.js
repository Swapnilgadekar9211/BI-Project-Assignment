//give i/p in celsius
const celsius = prompt("Enter a Temperature in degree celsius: ");
//it cal the temp in fahrenheit
const fahrenheit = (celsius * 1.8) + 32
//it display the result
console.log(`${celsius} degree celsius is converted to ${fahrenheit} degree fahrenheit.`);